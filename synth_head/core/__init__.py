"""Public API for synth_head.core.

Import submodules directly (e.g. ``from synth_head.core.variation import ...``).
Avoid eager re-exports here so config UI and pytest can load individual modules
without pulling in Blender-only dependencies.
"""
